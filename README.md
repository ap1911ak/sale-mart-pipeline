# 🛒 Sale Mart Pipeline (Data Engineering Project)

[![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)](https://www.python.org/)
[![PostgreSQL](https://img.shields.io/badge/Database-PostgreSQL-336791.svg)](https://www.postgresql.org/)

โปรเจกต์นี้เป็นการจำลองระบบ **Data Pipeline** สำหรับร้านค้าปลีก (Sale Mart) เพื่อดึงข้อมูลจากแหล่งต่างๆ เข้าสู่ระบบฐานข้อมูลเพื่อการวิเคราะห์ (Data Warehousing) โดยเน้นกระบวนการ ETL ที่มีประสิทธิภาพและเป็นระบบ

---

## 📌 ภาพรวมโปรเจกต์ (Project Overview)
Sale Mart Pipeline ถูกออกแบบมาเพื่อจัดการข้อมูลการขาย สินค้า และลูกค้า โดยครอบคลุมกระบวนการหลักดังนี้:
* **Extraction:** ดึงข้อมูลดิบจากไฟล์ต้นทาง (CSV/JSON)
* **Transformation:** การทำความสะอาดข้อมูล (Cleaning) และเปลี่ยนโครงสร้างข้อมูล (Transform) ให้พร้อมใช้งาน
* **Loading:** นำเข้าข้อมูลสู่ PostgreSQL Database ในรูปแบบ Schema ที่เหมาะสมกับการทำ Report



---

## 🏗️ สถาปัตยกรรม (Architecture)
ระบบทำงานตามขั้นตอนดังนี้:
1.  **Ingestion:** รับข้อมูลจากแหล่งข้อมูลภายนอกเข้าสู่ระบบ
2.  **Processing:** ใช้ Python (Pandas) ในการจัดการ Data Logic
3.  **Storage:** เก็บข้อมูลลงใน Database ที่ผ่านการ Normalize แล้ว

---

## 🛠️ เครื่องมือที่ใช้ (Tech Stack)
* **Language:** Python
* **Data Manipulation:** Pandas
* **Database:** PostgreSQL
* **ORM/Driver:** SQLAlchemy, Psycopg2

---

## 📂 โครงสร้างโฟลเดอร์ (Project Structure)
```text
├── data/               # ไฟล์ข้อมูลดิบ (Raw Data)
├── scripts/            # Python scripts สำหรับกระบวนการ ETL
├── sql/                # SQL scripts สำหรับสร้าง Table/Schema
├── .env.example        # ไฟล์ตัวอย่างสำหรับตั้งค่า Database Connection
├── main.py             # จุดเริ่มต้นการรัน Pipeline (Entry Point)
└── requirements.txt    # รายการ Library ที่ต้องใช้
