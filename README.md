🛒 Sale Mart Pipeline (Data Engineering Project)
โปรเจกต์นี้เป็นการจำลองระบบ Data Pipeline สำหรับร้านค้าปลีก (Sale Mart) เพื่อดึงข้อมูลจากแหล่งต่างๆ เข้าสู่ระบบฐานข้อมูลเพื่อการวิเคราะห์ (Data Warehousing) โดยเน้นกระบวนการ ETL ที่มีประสิทธิภาพและเป็นระบบ

📌 ภาพรวมโปรเจกต์ (Project Overview)
    Sale Mart Pipeline ถูกออกแบบมาเพื่อจัดการข้อมูลการขาย สินค้า และลูกค้า โดยครอบคลุมตั้งแต่กระบวนการ:
      Extraction: ดึงข้อมูลดิบจากไฟล์ CSV หรือ API
      Transformation: ทำความสะอาดข้อมูล (Data Cleaning) และจัดการรูปแบบให้ถูกต้อง
      Loading: นำเข้าข้อมูลสู่ PostgreSQL Database ในรูปแบบ Star Schema

🏗️ สถาปัตยกรรมข้อมูล (Data Architecture)
    อธิบาย Flow การทำงานของโปรเจกต์ เช่น: Raw Data (CSV) ➡️ Python ETL Script ➡️ Staging Area ➡️ PostgreSQL (Fact/Dimension Tables)

🚀 คุณสมบัติเด่น (Features)
    ✅ Automated ETL: ระบบดึงข้อมูลและประมวลผลอัตโนมัติ
    ✅ Data Validation: ตรวจสอบความถูกต้องของข้อมูลก่อนบันทึก
    ✅ Logging System: มีระบบจัดเก็บ Log เพื่อตรวจสอบความผิดพลาดของ Pipeline
    ✅ Scalability: ออกแบบฐานข้อมูลให้รองรับการเติบโตของข้อมูลในอนาคต

🛠️ เครื่องมือที่ใช้ (Tech Stack)
    Language: Python
    Libraries: Pandas, SQLAlchemy, Psycopg2
    Database: PostgreSQL
    Environment: Docker / Virtualenv

📂 โครงสร้างโฟลเดอร์ (Project Structure)
    Plaintext
    ├── data/               # ไฟล์ข้อมูลดิบ (Raw Data)
    ├── scripts/            # Python scripts สำหรับ ETL
    ├── sql/                # SQL scripts สำหรับสร้าง Table/Schema
    ├── .env.example        # ไฟล์ตัวอย่างสำหรับตั้งค่า Database
    ├── main.py             # จุดเริ่มต้นการรัน Pipeline
    └── requirements.txt    # รายการ Library ที่ต้องใช้

⚙️ วิธีการติดตั้งและรันโปรเจกต์ (Installation & Usage)
Clone repository:
    Bash
      git clone https://github.com/ap1911ak/sale-mart-pipeline.git
      cd sale-mart-pipeline
    
ติดตั้ง Libraries ที่จำเป็น:
    Bash
      pip install -r requirements.txt
      ตั้งค่า Environment Variables: สร้างไฟล์ .env และระบุรายละเอียดการเชื่อมต่อฐานข้อมูล:
          DB_HOST=localhost
          DB_NAME=salemart_db
          DB_USER=your_user
          DB_PASS=your_password

รัน Pipeline:
    Bash
        python main.py

📊 ผลลัพธ์และการวิเคราะห์ (Results)
    หลังจากรัน Pipeline ข้อมูลจะถูกจัดเก็บในรูปแบบ Star Schema ซึ่งพร้อมสำหรับการนำไปสร้าง Dashboard บน Power BI หรือ Tableau เพื่อดูอินไซท์การขายต่อไป
